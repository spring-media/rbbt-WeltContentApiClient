content_api.mode = "test"

s3 {
  author {
    file = "@author_file"
    bucket = "@author_bucket"
  }
}

api {
    theme_details_service {
        host = "localhost"
        endpoint = "/themes/%s"
    }

    theme_summary_service {
            host = "localhost"
            endpoint = "/themes"
        }
}
